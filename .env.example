# ─────────────────────────────────────────────
# MySpinBot Phase 0 — Environment Configuration
# Copy this file to ".env" and adjust values as needed.
# ─────────────────────────────────────────────

# Grafana Admin Credentials
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=changeme

# Optional: custom Grafana settings
# GF_SECURITY_ALLOW_EMBEDDING=true
# GF_USERS_ALLOW_SIGN_UP=false
# GF_SERVER_DOMAIN=grafana.myspinbot.local

# These go to frontend/.env.local for dev purposes

NEXT_PUBLIC_API_URL=https://api.myspinbot.local
