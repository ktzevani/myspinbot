{
    "Servers": {
        "1": {
            "Name": "MySpinBot",
            "Group": "Servers",
            "Port": 5432,
            "Username": "${POSTGRES_USER}",
            "Host": "postgres",
            "SSLMode": "prefer",
            "MaintenanceDB": "${POSTGRES_DB}"
        }
    }
}