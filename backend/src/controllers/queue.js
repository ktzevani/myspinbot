// Phase 1 placeholder
// Future BullMQ controller (no Redis yet)
export async function enqueueJob(job) {
  // Placeholder: will enqueue job in Phase 2
  console.log('enqueueJob called with', job);
  return { ok: true };
}
